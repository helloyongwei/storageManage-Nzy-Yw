const rootapi = '/api/v1/manage'

/**
 * 供应商api
 */
const suppliersUrl = `/suppliers`
const materialsUrl = '/materials'
const storehousesUrl = '/storehouses'
const departmentsUrl = '/departments'

module.exports = {
  rootapi,
  suppliersUrl,
  materialsUrl,
  storehousesUrl,
  departmentsUrl
}
